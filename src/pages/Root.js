/* import React from 'react';
import {
    BrowserRouter as Router,
    Switch,
    Route,
} from 'react-router-dom';
import PagesPokemonsSearch from './Pokemon/Search/Search';

const Root = () => {
    return (
        <Router>
            <Switch>
                <Route path="/" component={PagesPokemonsSearch} />            
            </Switch>
        </Router>
    );
}

export default Root;
 */